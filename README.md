# KOReader User Patches

## 2-custom-sort
- Sorts into reading, new, abandoned, and finished in that order
- Sorts those by series except finished which is sorted by series reversed
- But if a folder contains a subfolder then it just sorts everything in that folder by name
